mixin searchBar(keyword)
    form(id="form-search" action="")
        .input-group 
            input(type="text" placeholder="Enter key word" name="keyword" class="form-control" value=`${keyword ? keyword : ""}`)
            .input-group-append 
                button(class="btn btn-success" type="submit") Search